[project]
name = "moe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch==2.6.0",
    "triton==3.2.0",
    "tomli==2.2.1",
    "tiktoken==0.9.0",
    "numpy>=2.2.3",
]

[project.scripts]
moe = "moe:main"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
